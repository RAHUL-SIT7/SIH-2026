# Copy this file to .env and fill in for production (real weather data).
# Get a free key at https://openweathermap.org/api
OPENWEATHER_API_KEY=

# If left blank, weather_service.py automatically falls back to a
# deterministic simulated forecast so the demo still works offline.